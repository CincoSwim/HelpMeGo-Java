<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

    <com.google.android.material.appbar.AppBarLayout
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:theme="@style/AppTheme.AppBarOverlay" android:id="@+id/appBarLayout">

        <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?attr/colorPrimary"
                app:popupTheme="@style/AppTheme.PopupOverlay"
                app:title="HelpMeGo"/>

    </com.google.android.material.appbar.AppBarLayout>

    <Spinner

            android:layout_marginRight="15dp"
            android:layout_marginLeft="15dp"
            android:layout_marginTop="60dp"
            android:hint="@string/Where_would_you_like_to_go"
            android:padding="10dp"
            android:prompt="@string/Where_would_you_like_to_go"
            android:id="@+id/spinner" app:layout_anchor="@+id/spinner"
            android:layout_height="44dp" android:layout_width="391dp"/>
    <Button
            android:id="@+id/button_first"
            android:layout_width="373dp"
            android:layout_height="476dp"
            android:layout_gravity="center"
            android:text="@string/HelpMeGo"
            android:textColor="@android:color/white"
            android:textSize="30sp"
            android:textAllCaps="false"
            android:background="?attr/colorPrimary"

            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintHorizontal_bias="0.496"
    />

            <!--android:onClick="HelpMeGo"-->
            <!--The value for this attribute
            must be the name of the method you want to call in
            response to a click event. The Activity hosting the layout
            must then implement the corresponding method.-->

    <Button
            android:id="@id/button_second"
            android:layout_width="373dp"
            android:layout_height="68dp"
            android:layout_marginEnd="20dp"
            android:layout_marginStart="20dp"
            android:layout_marginTop="630dp"
            android:text="@string/Help_and_About"
            android:textAllCaps="false"

            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintHorizontal_bias="0.496"/>
    <Button
            android:text="Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:id="@+id/BTButton" app:layout_anchorGravity="top|center"
            app:layout_anchor="@+id/button_second" android:visibility="gone"/>

    <!--android:onClick="HelpMeGo"-->
            <!--The value for this attribute
            must be the name of the method you want to call in
            response to a click event. The Activity hosting the layout
            must then implement the corresponding method.-->



</androidx.coordinatorlayout.widget.CoordinatorLayout>